import React from "react";import { Text, View, StyleSheet } from 'react-native';import { Card, Title } from 'react-native-paper';export const RecipeCard = ({ title, calories, time, imageUrl, onClick}) => {    return (        <Card style={styles.container} onPress={onClick}>            <Card.Cover source={{ uri: imageUrl }} style={styles.media}  />            <Card.Content style={styles.content}>                <Title style={styles.heading}>{title}</Title>                <Text style={styles.subheading}>{calories} calories   {time}</Text>            </Card.Content>        </Card>    );}export default RecipeCard;const styles = StyleSheet.create({    container: {        marginBottom: 12,        borderColor: '#d7d7d7',        borderBottomWidth: 0.5,    },    subheading: {        color: '#707070',        fontSize: 13    },    heading: {        fontSize: 22    },    media: {        height:130    }});